[package]
name = "codebaseviewer"
version = "0.1.0"
edition = "2024"

[dependencies]
ignore = "0.4"
percent-encoding = "2"
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
strip = true
