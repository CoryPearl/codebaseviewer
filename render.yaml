services:
  - type: web
    name: codebaseviewer
    runtime: rust
    plan: free
    buildCommand: cargo build --release --locked
    startCommand: ./target/release/codebaseviewer
    healthCheckPath: /api/health
