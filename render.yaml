services:
  - type: web
    name: codebaseviewer
    runtime: rust
    plan: free
    buildCommand: cd backend && cargo build --release --locked
    startCommand: ./backend/target/release/codebaseviewer
    healthCheckPath: /api/health
